JDBC프로그램 실행 순서
1.JDBC드라이버 로딩
1)JDBC드라이버 - DBMS와의 통신을 담당하는 자바클래스(.jar파일 형태)

	try{	
	Class.forName("org.mariadb.jdbc.Driver)";
	}catch(ClassNotFoundException e){
	}
2)JDBC URL
	2-1)jdbc:mysql://localhost:3306/sample";

2.데이터베이스 커넥션 구함
	conn = DriverManager.getConnection(url,user,password);	
	}catch(SQLException e){
	} (DriverManager.getConnection()메서드는 Connection객체를 

3.쿼리실행을 위한 Statement 객체 생성
	ResultSet executeQuery(String query) : SELECT 쿼리를 실행한다.
	3-1)executeQuery()메서드는 SELECT쿼리의 결과값을 java.sql.ResultSet 객체에 저장해서 리턴 한다.
	int executeUpdate(String query) : INSERT,UPDATE,DELETE쿼리를 실행한다.

4.쿼리실행

5.쿼리 실행 결과 사용

6.Statement 종료

7.데이터베이스 커넥션 종료